# Backlog 横断ガントチャート（CrossGantt for Backlog）設計ドキュメント

- ステータス: v1 実装済み
- 最終更新: 2026-09-10

> 設計時のドラフトを、実装で確定した内容に合わせて更新したものです。設計時に「要検証」としていた項目の結論は §13 にまとめています。

---

## 1. 背景と課題

Backlog には標準のガントチャート機能がありますが、**1 プロジェクト単位でしか表示できません**。そのため、以下のような状況で全体像を把握できません。

- 1 人のメンバーが複数プロジェクトを掛け持ちしており、そのメンバーの負荷やスケジュールの重なりが見えない
- マネージャーが担当する複数プロジェクトを横並びで比較できない
- プロジェクトをまたいだ期日の衝突・遅延が、各プロジェクトのガントを開き直さないと分からない

一方で Backlog API v2 の課題検索エンドポイント `GET /api/v2/issues` は `projectId[]` と `assigneeId[]` を**複数指定できる**ため、横断的な課題取得自体は API 側でサポートされています。本アプリはこの点に着目し、「複数プロジェクト × 複数ユーザーをまとめて 1 枚のガントチャートに描画する」ビューアを提供します。

## 2. 提供価値（このアプリで実現すること）

1. **横断表示**: 複数プロジェクト・複数担当者の課題を 1 枚のタイムラインに描画する
2. **担当者軸での可視化**: 「プロジェクト別」だけでなく「担当者別」に行をグルーピングし、個人の稼働の重なりを見えるようにする
3. **遅延の即時把握**: 期限超過かつ未完了の課題を視覚的に強調する
4. **共有可能な URL**: 表示条件（対象プロジェクト・担当者・期間・グルーピング）を URL に載せ、チーム内でそのままリンク共有できる

## 3. スコープ

### やること

- Backlog の OAuth 2.0 によるログイン（単一スペース）
- プロジェクト・担当者・期間・ステータスによるフィルタ
- 担当者別 / プロジェクト別 / マイルストーン別のグルーピング表示
- 日 / 週 / 月のズーム切り替え
- 遅延課題のハイライトと、担当者ごとのサマリー（件数・遅延件数）
- 課題バーのクリックで Backlog の該当課題を新規タブで開く
- 表示条件の URL 共有
- Cloudflare Workers へのデプロイ

### やらないこと（スコープ外）

| 項目                                           | 理由                                                                           |
| ---------------------------------------------- | ------------------------------------------------------------------------------ |
| ガント上でのドラッグによる期日変更（書き込み） | 読み取り専用と決定。更新系 API を使わないことで事故と実装コストを避ける        |
| 複数 Backlog スペースの横断                    | 1 セッションにつき 1 スペース。ユーザー同一性の名寄せ問題も発生するため        |
| 課題間の依存関係（先行 / 後続）の矢印描画      | Backlog に順序依存関係の概念が存在しないため。表現できるのは親子課題の階層のみ |
| クリティカルパス算出・リソース平準化           | 上記の依存関係が取得できないため成立しない                                     |
| 日本の祝日の背景表示                           | 外部データ依存になるため v1 は土日のみ。将来拡張とする                         |

## 4. 想定ユーザーとユースケース

| ユーザー                 | ユースケース                                                                |
| ------------------------ | --------------------------------------------------------------------------- |
| プロジェクトマネージャー | 自分が見ている複数プロジェクトを 1 画面で比較し、期日の衝突を早期に発見する |
| チームリード             | メンバーごとに行を並べ、特定メンバーに負荷が集中していないかを確認する      |
| メンバー本人             | 自分に割り当てられた全プロジェクトの課題を時系列で俯瞰し、着手順を決める    |

## 5. 画面構成

### 5.1 画面一覧

1. **接続設定画面** — Backlog スペースドメインと API キーを入力し、接続を検証する
2. **ガント画面** — メイン画面。フィルタバー + サマリー + ガントチャート

### 5.2 ガント画面のワイヤーフレーム

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ CrossGantt   space: example.backlog.jp        [接続設定] [再読込] [URLをコピー] │
├──────────────────────────────────────────────────────────────────────────────┤
│ プロジェクト[▼ 3件選択] 担当者[▼ 5件選択] 期間[2026-09-01〜2026-12-31]         │
│ ステータス[未対応/処理中/処理済み] □完了を含む  グルーピング[担当者別 ▼]        │
│ ズーム[日|週|月]  キーワード[__________]                                       │
├──────────────────────────────────────────────────────────────────────────────┤
│ サマリー: 対象 128件 / 遅延 7件 / 日付未設定 12件                              │
├─────────────────────┬────────────────────────────────────────────────────────┤
│ 行ヘッダ（固定）     │ 2026年9月         │ 2026年10月        │ 2026年11月     │
│                     │ 1  8  15  22  29  │ 6  13  20  27     │ 3  10  17  24  │
├─────────────────────┼────────────────────────────────────────────────────────┤
│ ▼ 山田太郎 (24件/2遅延)                    ┊today                            │
│   [PJ-A] ログイン改修│    ▓▓▓▓▓▓▓▓         ┊                                 │
│   [PJ-B] API設計     │        ▓▓▓▓▓▓▓▓▓▓▓▓ ┊                                 │
│   [PJ-A] 障害対応    │  ▒▒▒▒(遅延)         ┊                                 │
│ ▼ 佐藤花子 (18件/0遅延)                    ┊                                  │
│   [PJ-C] 画面実装    │           ▓▓▓▓▓▓▓▓▓▓┊▓▓▓▓                             │
│ ▶ 日付未設定 (12件)  │                     ┊                                  │
└─────────────────────┴────────────────────────────────────────────────────────┘
```

### 5.3 描画ルール

| ケース                              | 描画                                                                                               |
| ----------------------------------- | -------------------------------------------------------------------------------------------------- |
| `startDate` と `dueDate` の両方あり | 期間バーとして描画                                                                                 |
| `dueDate` のみ                      | 期限日 1 日分のマーカー（◆）として描画                                                             |
| `startDate` のみ                    | 開始日から本日までのバー（末端を破線で表現）                                                       |
| どちらも無し                        | タイムラインに描画せず、「日付未設定」セクションに集約                                             |
| `dueDate < 本日` かつ未完了         | バーを警告色にし、行ヘッダに遅延バッジを表示                                                       |
| 親子課題                            | 同一プロジェクト内では親課題の下に子課題をインデント表示（グルーピングがプロジェクト別のときのみ） |
| マイルストーン                      | プロジェクト別グルーピング時に、`releaseDueDate` を縦マーカーとして重ねる                          |

- バーの色はプロジェクトごとに固定色を割り当てる（プロジェクト ID からハッシュして安定的に決定）
- ホバー時のツールチップに、課題キー・件名・期間・ステータス・担当者・予定工数 / 実績工数を表示する
- 今日の位置に縦線を引く。土日は背景を淡いグレーにする

## 6. 技術構成

既存スターター（Hono + Vite + React + Cloudflare Workers）をそのまま使います。

| レイヤー     | 採用技術                                  | 備考                                        |
| ------------ | ----------------------------------------- | ------------------------------------------- |
| ランタイム   | Cloudflare Workers                        | `npm run deploy` でそのまま公開できる       |
| サーバー     | Hono                                      | API プロキシ + SSR                          |
| クライアント | React 19 + Vite                           | スターター既定                              |
| ガント描画   | 自前実装（CSS Grid + 絶対配置バー）       | 後述の理由によりライブラリを使わない        |
| 状態         | URL クエリパラメータ + React state        | 共有可能にするため表示条件は URL を正とする |
| 永続化       | ブラウザの `localStorage`（接続情報のみ） | サーバー側に永続ストレージを持たない        |

### ガントライブラリを使わない理由

- 数百行規模の描画になるため、行の仮想化と横スクロール同期を自前で制御したい
- React 19 との互換性が保証されたガントライブラリが限られる
- v1 は読み取り専用でありドラッグ編集が不要なため、必要な描画ロジックが「日付 → x 座標の線形変換」に収まる

x 座標は `left = (startDate - rangeStart) / dayMs * pxPerDay`、`width = (dueDate - startDate + 1day) / dayMs * pxPerDay` で決まります。ズーム切り替えは `pxPerDay` を変えるだけです。

### Durable Objects / Agents SDK について

スターターに含まれる `CounterAgent` は本アプリでは使用しません。実装時に削除するか、そのまま未使用として残すかは実装フェーズで判断します（AI 機能を将来足す場合は再利用の余地があります）。

## 7. Backlog API 連携設計

### 7.1 使用するエンドポイント

すべて `https://{space}/api/v2/...` に対し、`Authorization: Bearer <アクセストークン>` を付与して呼び出します。

| 用途                             | エンドポイント                                   | レート制限区分 |
| -------------------------------- | ------------------------------------------------ | -------------- |
| API キー検証・自分の情報取得     | `GET /api/v2/users/myself`                       | Read           |
| プロジェクト一覧                 | `GET /api/v2/projects?archived=false`            | Read           |
| プロジェクトの参加ユーザー一覧   | `GET /api/v2/projects/{projectIdOrKey}/users`    | Read           |
| プロジェクトのステータス一覧     | `GET /api/v2/projects/{projectIdOrKey}/statuses` | Read           |
| プロジェクトのマイルストーン一覧 | `GET /api/v2/projects/{projectIdOrKey}/versions` | Read           |
| 課題検索（本体）                 | `GET /api/v2/issues`                             | **Search**     |
| 課題件数                         | `GET /api/v2/issues/count`                       | **Search**     |
| レート制限状況                   | `GET /api/v2/rateLimit`                          | Read           |

### 7.2 課題検索のパラメータ設計

`GET /api/v2/issues` に対して以下を組み立てます。

| パラメータ                      | 値                                     | 意図                                     |
| ------------------------------- | -------------------------------------- | ---------------------------------------- |
| `projectId[]`                   | 選択中のプロジェクト ID 群             | 複数プロジェクト横断の要                 |
| `assigneeId[]`                  | 選択中の担当者 ID 群（未選択なら省略） | 複数ユーザー横断の要                     |
| `statusId[]`                    | 選択中のステータス ID 群               | 既定では完了以外                         |
| `dueDateSince` / `dueDateUntil` | 表示期間                               | 期間で絞る                               |
| `count`                         | `100`（API 上限）                      | 1 リクエストあたりの取得件数を最大化する |
| `offset`                        | `0, 100, 200, ...`                     | ページング                               |
| `sort`                          | `dueDate`                              | 安定した順序でページングするため         |
| `order`                         | `asc`                                  | 同上                                     |
| `keyword`                       | 任意                                   | キーワード検索                           |

#### 期間フィルタの設計（実装で確定した方式）

`startDateSince/Until` と `dueDateSince/Until` は AND 条件です。素直に `dueDateSince`／`dueDateUntil` だけを指定すると、表示期間を「またいでいる」課題（開始日が期間より前で、期限日が期間より後）を取りこぼします。

区間の重なり条件は `startDate <= 表示期間の終了日 かつ dueDate >= 表示期間の開始日` です。これはそのまま `startDateUntil` と `dueDateSince` の組み合わせで表現できます。ただしこの条件は、開始日・期限日のどちらかが未設定の課題を除外してしまいます。そこで実装では次の 3 本のクエリを投げ、課題 ID でマージして重複排除します。

| #   | 条件                                               | 拾える課題                                                                     |
| --- | -------------------------------------------------- | ------------------------------------------------------------------------------ |
| 1   | `startDateUntil = to` かつ `dueDateSince = from`   | 開始日・期限日の両方を持ち、表示期間と重なる課題（期間をまたぐ長期課題を含む） |
| 2   | `dueDateSince = from` かつ `dueDateUntil = to`     | 期限日が表示期間内にある課題（開始日が未設定のもの）                           |
| 3   | `startDateSince = from` かつ `startDateUntil = to` | 開始日が表示期間内にある課題（期限日が未設定のもの）                           |

**既知の取りこぼし**: 「開始日のみが設定されていて、その開始日が表示期間より前」の課題はどのクエリにも掛かりません。この課題は開始日から今日までのバーとして描かれるため、理屈のうえでは表示されるべきですが、v1 では許容します（期間を広げれば表示されます）。

`startDate` も `dueDate` も未設定の課題は、どの日付条件でも除外されます。これを絞り込む API パラメータは存在しないため、「日付未設定を含む」を有効にしたときだけ日付条件なしの 4 本目を投げ、取得結果から両方が空のものだけを残します。

### 7.3 ページング

`count` の上限は 100 です。実装ではまず `GET /api/v2/issues/count` で件数を取得し、必要なページ数を算出してから `offset` を並列に投げます（逐次にページを進めるより待ち時間が短くなるため）。件数が 0 のクエリではページ取得を行いません。

暴走防止のため 1 クエリあたり 25 ページ（2,500 件）を上限とし、打ち切った場合は UI に「件数が多いため一部のみ表示しています」と明示します。**黙って切り捨てないこと**を設計上の要件とします。

### 7.4 レート制限への対応

Backlog API のレート制限は Read / Update / **Search** / Icon の 4 区分に分かれており、区分ごとに 1 分あたりの上限があります。上限値はプラン（Free / 有料）によって異なるため、ハードコードせず `GET /api/v2/rateLimit` で取得します。公式ドキュメントの例では Search が 150 req/min となっています。

課題検索は Search 区分に該当し、本アプリで最も多く呼ぶエンドポイントです。以下で対応します。

- 課題取得の並列度を最大 5 に制限する（クエリ本数の並列度は 2）
- レスポンスヘッダ `X-RateLimit-Limit` / `X-RateLimit-Remaining` / `X-RateLimit-Reset` を記録する
- HTTP 429 を受けたら `Retry-After` または `X-RateLimit-Reset` を見て待機し、リトライする（既定 2 回まで）
- ただし待機が 8 秒を超える場合はリトライせず 429 をそのままクライアントへ返す。1 分待つとリクエストがタイムアウトして体験が悪化するため、UI で「しばらく待ってから再読込してください」と案内するほうが良いと判断した
- UI には 1 回の読み込みで消費した Backlog API リクエスト数を表示する

**負荷見積もり**: 10 プロジェクト × 平均 300 課題 = 3,000 件 → `count=100` で 30 リクエスト。Search 区分 150 req/min に対して 1 回の読み込みで 30 消費する計算になり、連続リロードには耐えられないため後述のキャッシュが重要になります。

### 7.5 ステータスの扱い

Backlog はプロジェクトごとにカスタムステータスを追加できるため、ステータス ID を `4 = 完了` のように決め打ちしてはいけません。選択中の各プロジェクトについて `GET /api/v2/projects/{id}/statuses` を呼び、以下のように扱います。

- フィルタ UI では**ステータス名**で束ねて表示する（複数プロジェクトで同名 ID 違いが存在しうるため）
- API に渡すときは、選択されたステータス名に対応する全プロジェクト分の ID を `statusId[]` に展開する
- 「完了」判定は次のヒューリスティックで行う。**Backlog API のステータスには完了フラグが存在しない**（`id` / `projectId` / `name` / `color` / `displayOrder` のみ）ため、次善の策として実装した
  - `id === 4`（スペース共通の既定ステータス。カスタムステータスには 5 以降が割り当てられる）
  - または名称が `完了` / `Closed`（前後の空白と大文字小文字は無視）
- 上記で判定できない独自の完了ステータスがある環境では、ステータスフィルタでユーザーが明示的に選択できる

### 7.6 担当者一覧の取得

選択中プロジェクトそれぞれについて `GET /api/v2/projects/{id}/users` を呼び、ユーザー ID で重複排除して統合したものを担当者フィルタの選択肢とします。スペース全体の `GET /api/v2/users` ではなくプロジェクト単位にすることで、無関係なユーザーが候補に並ぶのを防ぎます。

## 8. サーバー（Worker）API 設計

### 8.1 なぜプロキシが必要か

Backlog API はブラウザからのクロスオリジンリクエストを想定しておらず、CORS ヘッダーが返らない前提で設計します（実装時に要検証。仮に許可されていたとしても、レート制限の集中管理とキャッシュのためにプロキシを経由させる方針は変えません）。

### 8.2 エンドポイント

| メソッド | パス                             | 説明                                                                 |
| -------- | -------------------------------- | -------------------------------------------------------------------- |
| POST     | `/api/connect`                   | API キーを検証し、`users/myself` の結果を返す                        |
| GET      | `/api/projects`                  | プロジェクト一覧                                                     |
| GET      | `/api/members?projectIds=1,2,3`  | 対象プロジェクトの担当者候補（統合済み）                             |
| GET      | `/api/statuses?projectIds=1,2,3` | 対象プロジェクトのステータス（名前で統合済み）                       |
| GET      | `/api/issues?...`                | 課題取得。ページング・マージ・重複排除まで Worker 側で完了させて返す |
| GET      | `/api/rate-limit`                | レート制限の残量                                                     |

認証情報はリクエストヘッダで受け取ります。

```
X-Backlog-Space: example.backlog.jp
X-Backlog-Api-Key: <ユーザーのAPIキー>
```

クエリ文字列ではなくヘッダにするのは、Cloudflare のリクエストログやキャッシュキーに API キーが載るのを避けるためです。

### 8.3 セキュリティ上の必須対策

1. **オープンプロキシ化の防止**: `X-Backlog-Space` は許可ドメインのみを受け付ける。
   `^[a-z0-9][a-z0-9-]*\.(backlog\.jp|backlog\.com|backlogtool\.com)$` にマッチしないリクエストは 400 で拒否する。この検証を怠ると、本アプリが任意ホストへの中継器になります。
2. **API キーを保存・記録しない**: Worker 側で永続化せず、ログにも出さない。エラーメッセージにキーを含めない。
3. **キャッシュ分離**: 後述のキャッシュキーには API キーのハッシュ（SHA-256）を含め、別ユーザーのレスポンスが混ざらないようにする。
4. **エラーの転送**: Backlog からの 401 / 403 / 429 はステータスコードを保ったままクライアントへ返し、UI で適切なメッセージを出す。

## 9. データモデル

```ts
// Backlog から取得した課題を、描画に必要な形へ正規化したもの
type GanttIssue = {
  id: number
  issueKey: string // 例: "PJ-A-123"
  summary: string
  url: string // https://{space}/view/{issueKey}
  projectId: number
  projectKey: string
  assigneeId: number | null
  assigneeName: string | null
  statusId: number
  statusName: string
  isClosed: boolean
  startDate: string | null // yyyy-MM-dd
  dueDate: string | null // yyyy-MM-dd
  estimatedHours: number | null
  actualHours: number | null
  parentIssueId: number | null
  milestoneNames: string[]
  isOverdue: boolean // dueDate < today && !isClosed
}

// 表示条件（URL クエリと 1:1 対応させる）
type ViewFilter = {
  projectIds: number[]
  assigneeIds: number[]
  statusNames: string[]
  from: string // yyyy-MM-dd
  to: string // yyyy-MM-dd
  keyword: string
  groupBy: 'assignee' | 'project' | 'milestone'
  zoom: 'day' | 'week' | 'month'
  includeClosed: boolean
  includeNoDate: boolean
}
```

URL 例:

```
/?projects=101,102&assignees=5,8&from=2026-09-01&to=2026-12-31&group=assignee&zoom=week
```

## 10. 認証とデータの取り扱い

Backlog の OAuth 2.0（認可コードフロー）でログインします。

### フロー

```
ブラウザ                     Worker                         Backlog
   │  スペースを入力            │                               │
   ├── GET /api/auth/login ────▶│                               │
   │                            │ state を生成し KV に保存        │
   │                            │ state を HttpOnly Cookie にも  │
   │◀── 302 認可画面へ ─────────┤                               │
   ├────────────────────────────────────────────────────────────▶│
   │                            │            ユーザーが許可      │
   │◀───────────────── 302 /api/auth/callback?code=&state= ──────┤
   ├── GET /api/auth/callback ─▶│                               │
   │                            │ Cookie と KV の state を照合    │
   │                            ├── POST /api/v2/oauth2/token ─▶│
   │                            │◀── access / refresh token ────┤
   │                            ├── GET /api/v2/users/myself ──▶│
   │                            │ セッションを KV に保存          │
   │◀── 302 + cg_session Cookie ┤                               │
```

### 保存する場所

| 情報                                    | 保存先                                  | 備考                     |
| --------------------------------------- | --------------------------------------- | ------------------------ |
| アクセストークン / リフレッシュトークン | Cloudflare KV のセッション              | ブラウザには渡らない     |
| セッション ID                           | HttpOnly / Secure / SameSite=Lax Cookie | 有効期間 30 日           |
| OAuth の state                          | KV（10 分）と HttpOnly Cookie           | 二重照合して CSRF を防ぐ |
| 直前に入力したスペースドメイン          | ブラウザの localStorage                 | 秘密情報ではない         |

アクセストークンの寿命は 1 時間です。プロキシ API のミドルウェアで残り 60 秒を切っていたら
リフレッシュトークンで更新し、更新後のトークンを KV に書き戻します。

### 既知のリスク

- KV の値は暗号化されないため、KV への読み取り権限を持つ者はトークンを取得できます。保持するのは有効期限付きのトークンだけに留めています。
- `SameSite=Lax` のため、外部サイトからの GET 遷移では Cookie が送られます。本アプリの GET は副作用を持たないため実害はありませんが、更新系を追加する際は CSRF 対策を再検討する必要があります。
- 表示できる範囲はログインしたユーザーが参加しているプロジェクトに限られます（Backlog 側の権限がそのまま効きます）。

## 11. パフォーマンス設計

| 施策                | 内容                                                                                                                                                                                                                                             |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Worker 側キャッシュ | Cloudflare Cache API に 60 秒キャッシュ。キーは `ハッシュ(APIキー) + 正規化済みクエリ`。「再読込」ボタンではキャッシュをバイパスする                                                                                                             |
| 並列取得の制御      | 課題ページングの並列度を 5 に固定。プロジェクト単位の統計取得も同様                                                                                                                                                                              |
| 行の描画コスト削減  | 各行に CSS の `content-visibility: auto` と `contain-intrinsic-size` を指定し、ビューポート外の行のレイアウト・描画をブラウザに省略させる。JavaScript による仮想スクロールを実装せずに済み、スクロール位置の管理やアンカーリンクの破綻も起きない |
| 横スクロールの同期  | 行ヘッダは `position: sticky` で固定し、タイムラインのみスクロールさせる                                                                                                                                                                         |
| 初回表示            | 期間の既定値を「今月の 1 日 〜 3 か月後の末日」とし、初回の取得件数を抑える                                                                                                                                                                      |

## 12. 非機能要件

- 対応ブラウザ: 最新の Chrome / Edge / Safari / Firefox
- 画面幅 1280px 以上を主対象とする。スマートフォンは閲覧のみ可能な程度に留める（v1 では最適化しない）
- ダークモード対応（`prefers-color-scheme`）
- 課題 1,000 件までは体感 3 秒以内に描画できることを目標とする
- アクセシビリティ: バーはキーボードでフォーカス可能にし、`aria-label` に課題キー・件名・期間を持たせる

## 13. 未確定事項の扱い

設計時に挙げた検証項目のうち、実装で決着したものと、実データでの確認が残っているものを分けて記載します。

### 決着したもの

| #   | 項目                       | 結論                                                                                                                                                                                                                                                          |
| --- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Backlog API の CORS 可否   | 検証するまでもなくプロキシ方式を採るため、実装上の分岐にはならなかった。Worker 経由に一本化している                                                                                                                                                           |
| 2   | 完了ステータスの判定方法   | Backlog API のステータスに完了フラグは存在しない（`id` / `projectId` / `name` / `color` / `displayOrder` のみ）。`id === 4` または名称 `完了` / `Closed` によるヒューリステックとし、判定できない環境ではステータスフィルタで明示選択させる方針にした（§7.5） |
| 3   | 期間をまたぐ課題の取得漏れ | `startDateUntil` + `dueDateSince` で区間の重なり条件を表現できることが分かり、3 本のクエリをマージする方式に確定した（§7.2）                                                                                                                                  |
| 4   | 日付表現の揺れ             | Backlog の日付項目は JST 深夜に相当する UTC タイムスタンプで返ることがあるため、常に JST へ直してから日付部分を取り出す実装にした。UTC 深夜で返る環境でも同じ結果になることをテストで担保している                                                             |

### 実データでの確認が残っているもの

| #   | 項目                       | 対応方針                                                                                                                   |
| --- | -------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| A   | `offset` の実質的な上限    | 大きな `offset` での挙動は未確認。劣化するようであれば `MAX_PAGES_PER_QUERY`（現在 25）を引き下げる                        |
| B   | レート制限の実値           | プランによって異なる。`GET /api/v2/rateLimit` を叩く `/api/rate-limit` を用意済みだが、UI への常時表示までは実装していない |
| C   | 遅延課題の量               | 想定より多い場合、遅延ハイライトが画面を埋め尽くす可能性がある。実データを見てから表示を調整する                           |
| D   | 「日付未設定を含む」の負荷 | 日付条件なしのクエリを 1 本追加するため、課題数の多いプロジェクトでは打ち切りが発生しやすい。実データで頻度を確認する      |

## 14. 実装ステップ（すべて完了）

| #   | 内容                                                                                   | 状態 |
| --- | -------------------------------------------------------------------------------------- | ---- |
| 1   | 土台の整理（カウンターサンプル撤去、Agents SDK 依存の除去）                            | 完了 |
| 2   | 接続設定画面（`POST /api/connect` で検証し `localStorage` に保存）                     | 完了 |
| 3   | プロキシ API（マスタ系）`/api/projects` `/api/members` `/api/statuses`                 | 完了 |
| 4   | 課題取得 API `/api/issues`（件数取得 → ページ並列取得 → 3〜4 クエリのマージ → 正規化） | 完了 |
| 5   | ガント描画（期間バー・今日線・土日背景・ツールチップ・Backlog へのリンク）             | 完了 |
| 6   | フィルタとグルーピング（URL と同期）                                                   | 完了 |
| 7   | サマリーと遅延ハイライト                                                               | 完了 |
| 8   | 仕上げ（キャッシュ、エラー表示、ダークモード、README）                                 | 完了 |
| 9   | 静的解析・テスト（Biome / tsc / Vitest）                                               | 完了 |
| 10  | Cloudflare Workers へのデプロイ                                                        | 完了 |

### 品質チェックの構成

| コマンド                 | 内容                                       |
| ------------------------ | ------------------------------------------ |
| `npm run lint`           | Biome によるフォーマット検査と lint        |
| `npm run typecheck`      | `tsc --noEmit` による型チェック            |
| `npm test`               | Vitest によるユニットテストと描画テスト    |
| `npm run verify`         | 上記 3 つをまとめて実行                    |
| `node scripts/smoke.mjs` | 起動中のサーバーに対する簡易スモークテスト |

テストは日付演算・ガント描画ロジック・URL 変換・スペース検証・Backlog クライアント（リトライとキー masking）・課題取得のクエリ組み立て・プロキシ API のガード・React の初回描画を対象としています。Backlog の実データを叩くテストは含めていません。

## 15. 将来拡張

- ガントバーのドラッグによる `startDate` / `dueDate` の更新（`PATCH /api/v2/issues/{id}`、楽観的更新 + 失敗時ロールバック）
- 複数スペースの横断（スペースごとの接続情報管理とユーザー名寄せ）
- 日本の祝日の背景表示
- 画像 / PDF エクスポート、CSV エクスポート
- 担当者ごとの負荷ヒートマップ（`estimatedHours` を日割りして積み上げる）

## 参考

- Backlog API v2: 課題一覧の取得 — https://developer.nulab.com/docs/backlog/api/2/get-issue-list/
- Backlog API v2: プロジェクト一覧の取得 — https://developer.nulab.com/docs/backlog/api/2/get-project-list/
- Backlog API: レート制限 — https://developer.nulab.com/docs/backlog/rate-limit/
- Backlog API v2: レート制限の取得 — https://developer.nulab.com/docs/backlog/api/2/get-rate-limit/
