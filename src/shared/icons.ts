/**
 * 担当者アイコンの取得に関する、クライアントとサーバーの双方が守る取り決め。
 */

/**
 * 1 回の `icons` 呼び出しで扱うユーザー数の上限。
 *
 * アイコンは Backlog のレート制限のうち上限がとりわけ低い区分に属するため、
 * 1 度に頼む数を抑える。サーバーはこの数を超えた分を切り捨てるので、
 * クライアントが別の値を持つと「頼んだのに返らなかった」ぶんを取得失敗と
 * 覚えてしまい、そのアイコンは画面を開き直すまで出なくなる。
 * 上限は 1 か所に置き、両方がここを見る。
 */
export const MAX_ICONS_PER_CALL = 60
